# M5-20260106

Must remember git commands:

```
git add <filename> <filename2>
git commit -m "<Your message here>"
git push
```

Brief:

Create Architecture Diagram 
Create Project Plan/Kanban Board
Update ReadMe file.

Docker Commands:
docker build -t name .
docker run name

Docker Mount Commands:
docker run -v "$(pwd)/outputs:/data" hello-mount-v2 

End of the lesson notes - v2

